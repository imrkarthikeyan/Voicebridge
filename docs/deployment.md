# Deployment

Phase 1 provides Docker foundations for PostgreSQL, the Spring Boot backend, and the Vite frontend served by Nginx.

Production deployments must override all secrets and URLs through environment variables. Browser microphone access requires HTTPS except on localhost.
